openai==0.27.0         # Biblioteca oficial para trabalhar com a API do OpenAI (inclui Azure OpenAI)
python-dotenv==1.0.0   # Para carregar variáveis de ambiente do arquivo .env
requests==2.28.1       # Biblioteca para fazer requisições HTTP (necessária por dependência do openai)
